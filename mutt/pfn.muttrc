# vim: ft=muttrc
# accounts
set my_password="`(pass gmail/pfn-mutt)`"

## default
set imap_user="proprefenetre@gmail.com"
set imap_pass=$my_password
set smtp_url="smtp://proprefenetre@smtp.gmail.com:587/"
set smtp_pass=$my_password

set folder="imaps://imap.gmail.com:993"
mailboxes =INBOX 
mailboxes "+[Gmail]/All Mail" +[Gmail]/Drafts "+[Gmail]/Starred" +[Gmail]/Trash +[Gmail]/Spam "+[Gmail]/Sent Mail"

set spoolfile= +INBOX
set postponed="+[Gmail]/Drafts"
set record="+[Gmail]/Sent Mail"
